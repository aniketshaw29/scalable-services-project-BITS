apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-db
  namespace: campus-eventhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-db
  template:
    metadata:
      labels:
        app: event-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "event_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "event_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
          volumeMounts:
            - name: event-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: event-db-data
          persistentVolumeClaim:
            claimName: event-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: event-db-pvc
  namespace: campus-eventhub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: event-db
  namespace: campus-eventhub
spec:
  selector:
    app: event-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
  namespace: campus-eventhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      containers:
        - name: event-service
          image: campus-eventhub/event-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4071
          env:
            - name: DB_URL
              value: "jdbc:postgresql://event-db:5432/event_db"
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: DB_PASSWORD
            - name: EUREKA_URL
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: EUREKA_URL
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 4071
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 4071
            initialDelaySeconds: 80
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: campus-eventhub
spec:
  selector:
    app: event-service
  ports:
    - port: 4071
      targetPort: 4071
  type: ClusterIP
