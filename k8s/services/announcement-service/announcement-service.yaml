apiVersion: apps/v1
kind: Deployment
metadata:
  name: announcement-db
  namespace: campus-eventhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: announcement-db
  template:
    metadata:
      labels:
        app: announcement-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "announcement_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "announcement_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
          volumeMounts:
            - name: announcement-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: announcement-db-data
          persistentVolumeClaim:
            claimName: announcement-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: announcement-db-pvc
  namespace: campus-eventhub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: announcement-db
  namespace: campus-eventhub
spec:
  selector:
    app: announcement-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: announcement-service
  namespace: campus-eventhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: announcement-service
  template:
    metadata:
      labels:
        app: announcement-service
    spec:
      containers:
        - name: announcement-service
          image: campus-eventhub/announcement-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4080
          env:
            - name: DB_URL
              value: "jdbc:postgresql://announcement-db:5432/announcement_db"
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: DB_PASSWORD
            - name: RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: RABBITMQ_HOST
            - name: RABBITMQ_PORT
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: RABBITMQ_PORT
            - name: RABBITMQ_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: RABBITMQ_USERNAME
            - name: RABBITMQ_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: RABBITMQ_PASSWORD
            - name: EUREKA_URL
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: EUREKA_URL
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 4080
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 4080
            initialDelaySeconds: 80
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: announcement-service
  namespace: campus-eventhub
spec:
  selector:
    app: announcement-service
  ports:
    - port: 4080
      targetPort: 4080
  type: ClusterIP
