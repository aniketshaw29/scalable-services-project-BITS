apiVersion: apps/v1
kind: Deployment
metadata:
  name: venue-db
  namespace: campus-eventhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: venue-db
  template:
    metadata:
      labels:
        app: venue-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "venue_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "venue_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
          volumeMounts:
            - name: venue-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: venue-db-data
          persistentVolumeClaim:
            claimName: venue-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: venue-db-pvc
  namespace: campus-eventhub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: venue-db
  namespace: campus-eventhub
spec:
  selector:
    app: venue-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: venue-service
  namespace: campus-eventhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: venue-service
  template:
    metadata:
      labels:
        app: venue-service
    spec:
      containers:
        - name: venue-service
          image: campus-eventhub/venue-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4073
          env:
            - name: DB_URL
              value: "jdbc:postgresql://venue-db:5432/venue_db"
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: DB_PASSWORD
            - name: EUREKA_URL
              valueFrom:
                configMapKeyRef:
                  name: campus-config
                  key: EUREKA_URL
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 4073
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 4073
            initialDelaySeconds: 80
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: venue-service
  namespace: campus-eventhub
spec:
  selector:
    app: venue-service
  ports:
    - port: 4073
      targetPort: 4073
  type: ClusterIP
